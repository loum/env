# Ignore everything in this directory as we only need it for scafolding
# around the sphinx-build documenting tool.
# This file can be safely removed once we actually place tracked
# content within the directory.
*
# Except this file
!.gitignore
!.vimrc
!.vim/**/**
!.bash_aliases
!.bash_profile
!.bashrc
!.pylintrc
!.config
